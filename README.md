# SimpleKitchen

SimpleKitchen helps create, store and manage receipts.  
Simple, fast and pretty.

## Running the service on docker (installed via docker.io)

1. Copy `env` to `.env`
2. Fill the configs in `.env`
3. Run `deploy.sh` under sudo